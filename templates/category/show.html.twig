{% extends 'base.html.twig' %}

{% block title %}Catégorie : {{ category.title }}{% endblock %}

{% block importmap %}{{ importmap(['app', 'category']) }}{% endblock %}

{% block body %}
    {# {{ dump() }} #}

    <section class="category">
        <h1>Categorie : {{ category.title }}</h1>

        {# <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ category.id }}</td>
                </tr>
                <tr>
                    <th>Title</th>
                    <td>{{ category.title }}</td>
                </tr>
                <tr>
                    <th>Slug</th>
                    <td>{{ category.slug }}</td>
                </tr>
                <tr>
                    <th>Image</th>
                    <td>{{ category.image }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ category.description }}</td>
                </tr>
                <tr>
                    <th>SeoTitle</th>
                    <td>{{ category.seoTitle }}</td>
                </tr>
                <tr>
                    <th>SeoDescription</th>
                    <td>{{ category.seoDescription }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_category_index') }}">back to list</a>

        <a href="{{ path('app_category_edit', {'id': category.id}) }}">edit</a>

        {{ include('category/_delete_form.html.twig') }} #}

        {% for article in category.articles %}
            {% if article %}
                <h2 style="font-size: clamp(17px, 1.5vw, 22px); margin: 7px 0; color: var(--title-color); font-weight: 400; text-decoration: none;">
                    <a href="{{ path('app_article_show', {'slug': article.slug}) }}">{{ article.title }}</a>
                </h2>
            {% else %}
                <h2>Aucun article</h2>
            {% endif %}
        {% endfor %}
    </section>
{% endblock %}
