<header id="top">
    <nav>
        <ul class="menu mobile">
            <li><a class="logo" href="{{ path('app_home') }}">
                {{ settings.siteName }}
            </a></li>
            <li><a href="{{ path('app_article_index') }}">Blog</a></li>
            <li><a href="{{ path('app_contact') }}">Contact</a></li>
            <li><a href="{{ path('app_apropos') }}">A propos</a></li>
            <li><a href="{{ path('app_recherche') }}">Recherche</a></li>
            {% if app.user %}
                <li><a href="{{ path('admin') }}">Tableau de bord</a></li>
                <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
            {% else %}
                <li><a href="{{ path('app_login') }}">Connexion</a></li>
                <li><a href="{{ path('app_register') }}">S'enregistrer</a></li>
            {% endif %}
        </ul>
        <div class="burger">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </nav>
</header>