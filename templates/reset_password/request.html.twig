{% extends 'base.html.twig' %}

{% block title %}Réinisation du mot de passe{% endblock %}

{% block importmap %}{{ importmap(['app', 'reset-password']) }}{% endblock %}

{% block body %}
    <section class="reset-password">
        {% for flash_error in app.flashes('reset_password_error') %}
            <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
        {% endfor %}
        <h1>Reinitialisation du mot de passe</h1>

        {{ form_start(requestForm) }}
            {{ form_row(requestForm.email) }}
            <div>
                <small>
                    Entrez votre adresse email et nous vous enverrons un lien pour reinitialiser votre mot de passe.
                </small>
            </div>

            <button class="btn btn-primary">Envoyer le lien</button>
        {{ form_end(requestForm) }}
    </section>
{% endblock %}
